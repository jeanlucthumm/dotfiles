Open the target Slack message/thread in the Slack app: `open -a Slack "<permalink>"`. The permalink is usually already in conversation. Print it as a clickable fallback.

ARGUMENTS: which message/thread (defaults to the one under discussion)
